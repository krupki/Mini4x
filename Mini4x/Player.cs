public class Player : Unit
{
    public Player(string name, char symbol, ConsoleColor color, int startX, int startY) 
        : base(name, symbol, color, startX, startY)
    {
        // no special logic
    }
}